spring:  application:    name: wishlist  config:    import: "optional:file:.env"  jackson:    property-naming-strategy: SNAKE_CASE  data:    mongodb:      uri: ${MONGO_URL}  rabbitmq:    host: localhost    port: 5672    username: guest    password: guestserver:  port: ${WISHLIST_PORT}jwt:  secretKey: ${JWT_SECRET_KEY}