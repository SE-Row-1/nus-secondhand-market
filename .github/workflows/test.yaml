name: Build Docker Image

on:
  push:
    branches:
      - main
    paths:
      - 'services/web/**'
      - 'services/account/**'
      - 'services/item/**'
      - 'services/wishlist/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Filter file changes
      id: filter
      uses: dorny/paths-filter@v3
      with:
        filters: |
          web:
            - 'services/web/**'
          account:
            - 'services/account/**'
          item:
            - 'services/item/**'
          wishlist:
            - 'services/wishlist/**'

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    # Build and push Web services
    - name: Build and push Web services
      if: steps.filter.outputs.web == 'true'
      uses: docker/build-push-action@v6
      with:
        build-args: |
          NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
        context: ./services/web
        push: true
        tags: |
          z1yoon/nshm-web:${{ github.ref_name }}
          z1yoon/nshm-web:${{ github.sha }}

    # Build and push Account services
    - name: Build and push Account services
      if: steps.filter.outputs.account == 'true'
      uses: docker/build-push-action@v6
      with:
        context: ./services/account
        push: true
        tags: |
          z1yoon/nshm-account:${{ github.ref_name }}
          z1yoon/nshm-account:${{ github.sha }}

    # Build and push Item services
    - name: Build and push Item services
      if: steps.filter.outputs.item == 'true'
      uses: docker/build-push-action@v6
      with:
        context: ./services/item
        push: true
        tags: |
          z1yoon/nshm-item:${{ github.ref_name }}
          z1yoon/nshm-item:${{ github.sha }}

    # Build and push Wishlist services
    - name: Build and push Wishlist services
      if: steps.filter.outputs.wishlist == 'true'
      uses: docker/build-push-action@v6
      with:
        context: ./services/wishlist
        push: true
        tags: |
          z1yoon/nshm-wishlist:${{ github.ref_name }}
          z1yoon/nshm-wishlist:${{ github.sha }}

